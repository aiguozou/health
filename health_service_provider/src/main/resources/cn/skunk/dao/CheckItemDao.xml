<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="cn.skunk.dao.CheckItemDao">
    <insert id="add" parameterType="cn.skunk.pojo.CheckItem">
        insert into t_checkitem values(null,#{code},#{name},#{sex},#{age},#{price},#{type},#{attention},#{remark})
    </insert>

    <select id="findPage" parameterType="String" resultType="cn.skunk.pojo.CheckItem">
        select * from t_checkitem
        <where>
            <if test="value!=null and value.length>0">
                code=#{value} or name=#{value}
            </if>
        </where>
    </select>

    <select id="findCountById" parameterType="int" resultType="long">
        select count(*) from t_checkgroup_checkitem where checkitem_id=#{checkitem_id}
    </select>
    <delete id="deleteById" parameterType="int">
        delete from t_checkitem where id=#{id}
    </delete>

    <select id="findById" parameterType="int" resultType="cn.skunk.pojo.CheckItem">
        select * from t_checkitem where id=#{id}
    </select>

    <update id="edit" parameterType="cn.skunk.pojo.CheckItem">
        update t_checkitem
        <set>
            <if test="code!=null">
                code=#{code},
            </if>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="sex!=null">
                sex=#{sex},
            </if>
            <if test="age!=null">
                age=#{age},
            </if>
            <if test="price!=null">
                price=#{price},
            </if>
            <if test="type!=null">
                type=#{type},
            </if>
            <if test="attention!=null">
                attention=#{attention},
            </if>
            <if test="remark!=null">
                remark=#{remark}
            </if>
        </set>
        where id=#{id}
    </update>

    <select id="findAll" resultType="cn.skunk.pojo.CheckItem">
        select * from t_checkitem
    </select>
    
    <select id="findCheckItemById" resultType="cn.skunk.pojo.CheckItem">
        select * from t_checkitem where id in(select checkitem_id from t_checkgroup_checkitem where checkgroup_id=#{checkgroup_id})
    </select>
 </mapper>